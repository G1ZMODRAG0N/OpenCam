//! A generated RVA struct.

// DO NOT EDIT THIS FILE DIRECTLY.

/// A struct containing offsets (relative to the beginning of the executable) of
/// various addresses of structures and functions. They can be converted to a
/// usable address using the [Pe::rva_to_va] trait function of [Program].
///
/// These are populated from `mapper-profile.toml` in the root of this package
/// using `tools/binary-generator`.
pub struct RvaBundle {
    pub check_summoning_frame: u32,
    pub p2p_packet_dequeue: u32,
    pub p2p_send_packet: u32,
    pub sodium_kx_key_derive: u32,
    pub replenish_items_from_chest: u32,
    pub can_use_mp_item_by_index: u32,
    pub can_use_goods: u32,
    pub on_death_start: u32,
    pub respawn_player_on_arena: u32,
    pub get_last_load_pos: u32,
    pub chr_ctrl: u32,
    pub on_key_time2: u32,
    pub warp_character: u32,
    pub get_phantom_param_id: u32,
    pub apply_phantom_sfx: u32,
    pub warp_next_stage_kick: u32,
    pub all_no_arrow_consume: u32,
    pub show_net_notice_wkill: u32,
    pub open_chest_menu: u32,
    pub open_aow_menu: u32,
    pub broadcast_equip_change: u32,
    pub apply_speffect: u32,
    pub client_side_net_msg: u32,
    pub can_use_sign_puddle: u32,
    pub can_summon_red_sign: u32,
    pub can_summon_white_sign: u32,
    pub title_top_dialog: u32,
    pub summoning_frame: u32,
    pub update_friend_tag: u32,
    pub stage_rendered: u32,
    pub make_blood_msg: u32,
    pub attach_sfx: u32,
    pub get_and_format: u32,
    pub open_equip_menu: u32,
    pub leave_party_member: u32,
    pub is_mp_inprogress: u32,
    pub csfd4_single_sfx: u32,
    pub create_sign_sfx: u32,
    pub display_map_title: u32,
    pub bonfire_world_ctrl: u32,
    pub show_net_notice: u32,
    pub is_crafting_enabled: u32,
    pub black_death_event: u32,
    pub give_items: u32,
    pub open_menu_flag: u32,
    pub get_remote_player_capacity: u32,
    pub remove_rune_arc: u32,
    pub poll_input: u32,
    pub apply_zoom_lerp: u32,
    pub apply_control_movement: u32,
    pub get_block_coords: u32,
    pub item_used: u32,
    pub cam_x_speed: u32,
    pub cam_y_speed: u32,
    pub get_action_button_param: u32,
    pub wnd_proc: u32,
    pub cam_wall_collision: u32,
    pub field_area: u32,
    pub can_mn_player_use_goods: u32,
    pub apply_phantom_tint: u32,
    pub can_target_team_type: u32,
    pub get_target_info: u32,
    pub update_fe_values: u32,
    pub send_home: u32,
    pub sos_sign_man_update: u32,
    pub sos_sign_start_session: u32,
    pub get_release_flag: u32,
    pub open_cov_popup_menu: u32,
    pub open_goods_popup_menu: u32,
    pub build_equip_slot: u32,
    pub add_equip_menu_slot: u32,
    pub param_goods_get_entry: u32,
    pub is_tongued: u32,
    pub func_impl_vtable: u32,
    pub equip_item: u32,
    pub unequip_item: u32,
    pub run_tae_anim_queue: u32,
    pub fxr_set: u32,
    pub fade_to_color: u32,
    pub step_map_init: u32,
    pub is_remote_synced: u32,
    pub validate_packet12: u32,
    pub request_leave: u32,
}
